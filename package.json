{
  "name": "estadio-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd maintenance_app && npm install && npm run build",
    "start": "cd maintenance_app && npm start"
  }
}
